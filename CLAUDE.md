# Ledger

Osobista aplikacja do finansów ("Ledger"). Interfejs w języku polskim. Backend FastAPI + Postgres, frontend React + TypeScript + Vite.

## Stack

- **Backend**: FastAPI, SQLAlchemy, Alembic (migracje), Postgres 16 (Docker Compose, `docker-compose.yml`), auth ciasteczkowe (bcrypt + PyJWT).
  - `backend/app/main.py` - wejście aplikacji
  - `backend/app/routers/` - `auth.py`, `tabs.py`, `widgets.py`, `fx.py`, `pins.py`
  - `backend/app/models.py`, `schemas.py`, `service.py`, `security.py`, `deps.py`, `config.py`, `database.py`
- **Frontend**: React 19 + TypeScript + Vite 8, bez dodatkowej biblioteki CSS (własny `styles/theme.css`), ikony `lucide-react`, routing `react-router-dom`.
  - `frontend/src/pages/` - `AuthPage.tsx`, `AppPage.tsx` (główny widok)
  - `frontend/src/components/` - `Sidebar.tsx`, `TabModal.tsx`, `WidgetCard.tsx`, `WidgetSettingsModal.tsx`, `AddFieldModal.tsx`, `EntryModal.tsx`, `FieldsMasonry.tsx`, `FormulaBuilder.tsx`, `CurrencySelect.tsx`, `RateField.tsx`, `DragGhost.tsx`, `ThemeToggle.tsx`, `PinnedWidgetCard.tsx`, `PinPickerModal.tsx`, `SettingsModal.tsx`, `AvatarPicker.tsx`, `Avatar.tsx`
  - `frontend/src/hooks/useDragReorder.ts` - generyczny hook przeciągania (patrz niżej)
  - `frontend/src/lib/` - `format.ts`, `currencies.ts`, `evalFormula.ts`, `palette.ts` (kolory zakładek i awatara, współdzielone), `avatarIcons.ts` (gotowe kształty awatara)
  - `frontend/src/api/` - `client.ts` (fetch wrapper, `API_URL = http://localhost:8000`), `auth.ts`, `tabs.ts`, `widgets.ts`, `fx.ts`, `pins.ts`

## Model danych (skrót)

Użytkownik ma wiele **zakładek** (`Tab`: nazwa, kolor, pozycja), każda zakładka ma wiele **pól/widgetów** (`Widget`): typy `single_value`, `table`, `currency`, `formula`. Pole `currency` i `formula` mają `config` (JSON) do ustawień (np. kurs waluty, tokeny formuły). Pole `table` ma wpisy (`WidgetEntry`).

Jedna zakładka na użytkownika ma `is_home=true` ("Strona główna") - tworzona automatycznie przy rejestracji, nie da się jej usunąć ani przesunąć w kolejności (zawsze pierwsza), nie da się na niej tworzyć natywnych pól. Zamiast tego pokazuje **piny** (`Pin`: `user_id`, `widget_id`, `position`) - żywe odwołania do widgetów z innych zakładek (nie kopie), renderowane jako kompaktowe karty z samym wynikiem (patrz `PinnedWidgetCard.tsx`). Oba FK w `Pin` mają `ondelete=CASCADE`, więc usunięcie widgetu/zakładki/użytkownika samo sprząta piny.

`User` ma opcjonalne `name`, `avatar_color`, `avatar_icon` (wszystkie `nullable`, brak wartości = domyślny wygląd sprzed tej funkcji - gradient + inicjał e-maila). Bez uploadu zdjęcia - `avatar_color` to jeden z 7 kolorów z `lib/palette.ts` (ta sama paleta co kolory zakładek), `avatar_icon` to klucz do jednej z 9 gotowych ikon `lucide-react` w `lib/avatarIcons.ts` (albo `null` = litera-inicjał).

## Konwencje pracy

- **Cały UI po polsku**, w tym komentarze w kodzie dot. nietrywialnych decyzji (komentarze tylko tam, gdzie "dlaczego" nie jest oczywiste - bez opisowych komentarzy).
- Użytkownik akceptuje pragmatyczne, proste rozwiązania ("nic skomplikowanego") - unikać nadmiernej abstrakcji.
- **Zawsze testować realnie w przeglądarce przed zgłoszeniem "gotowe"**, zwłaszcza UI/mobile.
- **Dev server / Vite HMR bywa niestabilny w tej sesji** - jeśli po edycji coś nie działa mimo poprawnego kodu (błędy JSX-parse, "stare" zachowanie), otwórz **świeżą kartę** przeglądarki i przejdź do `http://localhost:5173` zamiast debugować dalej - to najczęściej rozwiązuje problem.
- **Sesja logowania nie przetrwa pełnego przeładowania strony** w środowisku testowym (ciasteczko ginie przy `navigate`) - po każdym pełnym reloadzie trzeba zalogować się ponownie.
- **`requestAnimationFrame` bywa mocno throttlowany/niemiarowy** w automatyzowanej przeglądarce testowej (Claude Browser pane) nawet gdy karta jest "visible" - testy logiki opartej o rAF (np. drag-and-drop) lepiej weryfikować realnym przeciągnięciem myszą (`computer` tool, `left_click_drag`) niż syntetycznymi `dispatchEvent(PointerEvent)` w pętli z `setTimeout` - te potrafią dawać fałszywe negatywy.
- Dane testowe (zakładki/pola) wygodnie tworzyć/usuwać przez bezpośrednie `fetch` do `http://localhost:8000/...` z konsoli przeglądarki (szybsze niż klikanie przez UI).
- Test przeciągania **persystuje do bazy** - powtórzenie tego samego przeciągnięcia dwa razy z rzędu bez resetu danych cofa zmianę i daje mylący wynik "nic się nie zmieniło".

## Historia commitów

```
6d2af47 Initial commit
8129001 Bootstrap FastAPI backend and Vite frontend
a6954f1 Add cookie-based auth and React auth pages
1abe203 Add tab/widget data model and API
8b5eaf2 Unify auth UI and add theme system
c33eb14 Add tab sidebar and management modal
ecd9507 Add widget field management UI and API
0eacb12 Add formula and currency widget UI
f51a5c2 Add FX rate fetch API and currency UI controls
1514e0e Add token-based formula builder and evaluator
cb7f866 Improve modal UX and formula input validation
6949357 Add masonry layout and widget drag reordering
7ec74ed Add edit mode with pointer drag reordering   <- patrz szczegóły niżej
```

### `7ec74ed` - "Add edit mode with pointer drag reordering" (szczegóły, nigdy wcześniej nie zapisane)

Duży, wieloetapowy commit obejmujący cały tryb edycji układu:

1. **Ujednolicenie ustawień pól** - zębatka (ustawienia w modalu) zostaje tylko dla `formula`/`currency`; dla `single_value`/`table` nazwę edytuje się inline w trybie edycji (ikona kosza do usuwania też inline).
2. **Naprawa z-index** modala nad mobilnym menu hamburgerowym - rozdzielenie `.scrim` (modale, z-index 50) i `.nav-scrim` (tło menu mobilnego, z-index 35), oba poniżej `.rail.is-open` (z-index 40) tam gdzie trzeba.
3. **Sticky pasek górny na mobilnym** (`.mobile-topbar`) - przyciski (hamburger, edycja, dodaj pole) zawsze widoczne przy scrollu.
4. **Stały layout na desktopie** - `.shell` na `height:100vh; overflow:hidden`, scrolluje się tylko `.main-content` i niezależnie `.rail-tabs`; sidebar i nagłówek pozostają na miejscu. Lekko zmniejszony `.main-header`. Osobny override w media query dla mobile (przywraca zwykły scroll strony).
5. **Przełącznik trybu edycji przeniesiony** z menu hamburgerowego na stały przycisk (ołówek) w `.mobile-topbar`, dostępny bez otwierania menu.
6. **Pełne przejście z natywnego HTML5 Drag and Drop na Pointer Events** (`frontend/src/hooks/useDragReorder.ts`) - natywne DnD w ogóle nie działa na dotyku. Jeden hook obsługuje myszkę i dotyk jednocześnie, używany zarówno do przeciągania kafelków (`WidgetCard`) jak i zakładek (`Sidebar`/`TabRow`).
7. **"Duszek" (ghost) przeciąganego elementu** (`DragGhost.tsx`) - migawka `outerHTML` renderowana przez portal do `document.body`, podąża za kursorem/palcem, nie jest przycięta przez scrollowalne kontenery.
8. **Auto-scroll** najbliższego scrollowalnego kontenera (lub okna) przy krawędzi podczas przeciągania.
9. **Wydajność na mobile** - dwie poprawki po zgłoszeniu lagów: (a) `activeWidgets` zmemoizowane (`useMemo`) w `AppPage.tsx`, żeby `FieldsMasonry` nie przeliczał całego układu masonry przy każdej aktualizacji stanu przeciągania; (b) `handlePointerMove` w hooku tylko zapisuje pozycję do refa - cała kosztowna praca (hit-testing, autoscroll, pozycja duszka) liczona raz na klatkę animacji (`requestAnimationFrame`), a nie przy każdym surowym zdarzeniu dotyku.
10. **Revert edycji zakładek do modala** - próbne inline-edytowanie nazwy/koloru zakładki w `Sidebar` zostało wycofane na wyraźną prośbę użytkownika; wrócił pełny `TabModal` (nazwa + kolor + usuwanie), a zębatka otwierająca go jest widoczna tylko w trybie edycji.

### Niezacommitowane (stan na teraz)

- **`frontend/src/components/Sidebar.tsx`** - przeciąganie zakładki w otwartym menu mobilnym (`.rail`, leży nad pełnoekranowym `.nav-scrim`) teraz **anuluje się automatycznie**, gdy wskaźnik/palec wyjdzie poza granice szuflady (`.rail`) - wcześniej przeciąganie "żyło dalej" nad przyciemnionym tłem (duszek wisiał, bez możliwości zmiany kolejności, ale gest się nie kończył). Dodano `railRef` + wrapper `handleTabDragPointerMove`, który przy wyjściu poza `.rail` wywołuje `dragControls.handlePointerUp` zamiast `handlePointerMove`, kończąc przeciąganie bez reorderu. Nie dotyczy przeciągania kafelków (`WidgetCard`) ani zakładek na desktopie poza tym samym mechanizmem granic.
- **`frontend/src/pages/AppPage.tsx`** - z otwartym mobilnym menu (`.rail.is-open`) dało się dalej przewijać kafelki pod przyciemnionym tłem (`.nav-scrim` samo w sobie nie blokuje scrolla, bo na mobile scrolluje się cała strona - `.shell`/`.main-content` mają tam `overflow: visible`, patrz pkt 4 w `7ec74ed` wyżej). Dodano `useEffect` na `navOpen`, który na czas otwarcia menu ustawia `document.body.style.overflow = 'hidden'` (przywracając poprzednią wartość przy zamknięciu/odmontowaniu). Zweryfikowane w przeglądarce na 375px z 14 polami w zakładce - scroll strony faktycznie zablokowany przy otwartym menu, wraca po zamknięciu.
- **`frontend/src/components/WidgetCard.tsx`** - stopka pola `single_value` pokazywała `dodano {entry.entry_date}` (ręcznie wpisywana data wpisu z modala, patrz `EntryModal.tsx`) zamiast realnej daty ostatniej aktualizacji - jeśli ktoś zmienił tylko kwotę, nie ruszając pola "Data", stopka pokazywała starą datę mimo świeżego zapisu. `table`/`formula`/`currency` już wcześniej poprawnie pokazywały `widget.updated_at` (backend bumpuje to pole w każdym create/update entry, patrz `routers/widgets.py`) - ujednolicono `single_value` do tego samego wzorca (`zaktualizowano {widget.updated_at}`). Zweryfikowane w przeglądarce: wpis z `entry_date` sprzed 3 tygodni, stopka mimo to poprawnie pokazuje dzisiejszą datę.
- **Pigułki składników formuły w kolorze zakładki źródłowej, nie bieżącej** - w kafelku formuły (`WidgetCard.tsx`) i w pasku formuły podczas edycji (`FormulaBuilder.tsx`) pigułki pól były zawsze w kolorze zakładki, na której jest sama formuła (`color` prop), więc np. formuła "A+B" gdzie A i B są z innych, kolorowo odróżnialnych zakładek, wyglądała jednolicie w kolorze zakładki formuły - nie było widać skąd biorą się składniki. Lista wyboru pól pod spodem (`field-picker-row`) już wcześniej poprawnie kolorowała kropkę wg `field.tabColor`. Naprawiono oba miejsca: `WidgetCard.tsx` dostaje teraz `widgetColors` (mapa `widget_id -> kolor jego zakładki`, budowana w `AppPage.tsx` tak samo jak istniejące `widgetLabels`) i koloruje nią pigułki w widoku; `FormulaBuilder.tsx` używa `field?.tabColor` (już dostępnego w `referenceFields`) zamiast jednolitego `color` w swoim pasku formuły. Zweryfikowane w przeglądarce (3 zakładki różnych kolorów, formuła A+B) - pigułki w obu miejscach poprawnie przyjmują kolor swojej zakładki źródłowej.
- **Nazwy nie mieszczące się w kafelku/modalu (dwa niezależne miejsca)**:
  1. Tytuł pola na kafelku (`.field-title` w `theme.css`) miał `overflow:hidden; text-overflow:ellipsis; white-space:nowrap`, ale jako dziecko flexa `.field-card-header` bez `flex`/`min-width:0` nigdy się realnie nie kurczył (domyślne `min-width:auto` flex-itemu = szerokość jego treści, ten sam mechanizm co opisany wyżej przy `.shell > main`) - długa nazwa po prostu rozpychała nagłówek kafelka zamiast się przycinać. Dodano `flex: 1; min-width: 0;` do `.field-title` - teraz wielokropek faktycznie działa.
  2. Selecty "Z waluty"/"Na walutę" (`CurrencySelect`, w `AddFieldModal.tsx` i `WidgetSettingsModal.tsx`) stały obok siebie po połowie szerokości wąskiego modala (460px, formularz waluty) - dłuższe nazwy walut (np. "Rand południowoafrykański", "Won południowokoreański") przycinały się w połowie słowa tuż przy strzałce selecta, bez wielokropka (natywny `<select>` w Chrome go tu nie stosuje). Zmieniono kontener obu selectów z rzędu (`flex-direction: row`, domyślnie) na kolumnę (`flex-direction: column`) w obu miejscach - każdy select dostaje pełną szerokość modala i wszystkie nazwy z listy się mieszczą. Zweryfikowane w przeglądarce na desktopie i 375px (najdłuższe nazwy z listy, brak przepełnienia - sprawdzone przez `scrollWidth` vs `clientWidth`).
  3. **Pigułki tokenów-pól w formule (`.token-field` w `theme.css`)** - to był właściwy przypadek, o który chodziło użytkownikowi (nie waluta z pkt. 2, mylnie zgłoszona wcześniej - zostaje jak jest). Klasa `.token-field` (używana i w `.token-strip` - widok gotowej formuły w `WidgetCard.tsx`, i w `.formula-strip` - budowanie formuły w `FormulaBuilder.tsx`) miała tylko `white-space: nowrap`, bez żadnego ograniczenia szerokości - długa nazwa pola (np. wieloznaniowa nazwa pola walutowego) rozpychała pigułkę do pełnej szerokości tekstu, a to z kolei pchało cały `.formula-strip`/modal do poziomego scrolla zamiast się zawinąć. Lista wyboru pól pod spodem (`.field-picker-name`) już wcześniej poprawnie miała `flex:1; min-width:0; text-overflow:ellipsis` - problem był tylko w samych pigułkach tokenów. Dodano `max-width: 200px; overflow: hidden; text-overflow: ellipsis;` do `.token-field` - działa jednym fixem w obu miejscach (widok i edycja), bo to wspólna klasa. Zweryfikowane w przeglądarce - kafelek formuły i modal ustawień z bardzo długą nazwą pola, oba przycinają się z wielokropkiem, bez poziomego scrolla modala.
- **Stała strona główna z przypiętymi polami** - nowa, spora funkcja (pełny plan w historii sesji). Jedna zakładka na użytkownika ma `Tab.is_home=true` ("Strona główna") - tworzona automatycznie przy rejestracji (`routers/auth.py`) i backfillowana dla istniejących kont w migracji `3707eb3f2230_add_pins_and_home_tab.py`. Backend: `list_tabs` sortuje `is_home DESC, position` (strona główna zawsze `tabs[0]`), `delete_tab` i `create_widget` (na tej zakładce) zwracają `400` - nie da się jej usunąć ani dodać na niej natywnego pola. Rename/kolor przez zębatkę są dozwolone. Nowy model `Pin` (`routers/pins.py`, `schemas.PinOut`) - żywe odwołanie (`user_id`, `widget_id`, `position`, oba FK `ondelete=CASCADE`) do widgetu z innej zakładki, nie kopia; `service.pin_to_out` doszywa dane zakładki ŹRÓDŁOWEJ (`tab_id/tab_name/tab_color`) do pokolorowania karty, ten sam pomysł co pigułki formuły wyżej.
  - Frontend: `AppPage.tsx` rozdziela `tabs` na `homeTab`/`regularTabs` (tylko `regularTabs` biorą udział w `tabDrag`), dociąga `pins` obok `tabs` w `refreshTabs` (piny to żywe odwołania, więc muszą się odświeżać przy każdej zmianie widgetu, nie tylko własnej). Na stronie głównej `FieldsMasonry` renderuje `PinnedWidgetCard.tsx` (kompaktowa karta - tylko `widget.value`, bez listy wpisów, nawet dla `table`; klik przenosi na zakładkę źródłową) zamiast `WidgetCard`; przycisk "Dodaj pole" zamienia się na "Przypnij pole", otwierające `PinPickerModal.tsx` (lista niedopiętych jeszcze widgetów, stylistyka `field-picker-row` z `FormulaBuilder`). `Sidebar.tsx` renderuje `homeTab` jako osobny, stały wiersz nad listą zakładek (nowy prop `draggable` na `TabRow`, `false` dla strony głównej - bez uchwytu przeciągania nawet w trybie edycji). `FieldsMasonry.tsx` uogólniony na `<T extends {id: number}>` (był sztywno pod `Widget[]`), żeby dało się go użyć i dla `Pin[]`.
  - **Pułapka po drodze**: `FieldsMasonry` bez `key` przy przełączaniu między stroną główną (`Pin[]`) a zwykłą zakładką (`Widget[]`) zachowywał stary stan `columns` przez jeden render (React reużywa instancję komponentu w tym samym miejscu drzewa), więc `WidgetCard` dostawał obiekt `Pin` zamiast `Widget` i wywalał się na `widget.entries[0]` (`entries` nie istnieje na `Pin`). Naprawione przez `key={activeTab.id}` na obu użyciach `<FieldsMasonry>` w `AppPage.tsx` - wymusza remount przy każdej zmianie aktywnej zakładki.
  - **Uwaga o środowisku**: w trakcie tej pracy backend (`uvicorn --reload`) przestał podejmować zmiany w kodzie (nowe endpointy dawały 404 mimo poprawnego `main.py`) - przyczyną było kilka osieroconych, starszych procesów nasłuchujących na porcie 8000 sprzed sesji (widoczne w `netstat`/`Get-NetTCPConnection`, ale nie w `Get-CimInstance Win32_Process` - jakby "duchy"), które przejmowały część ruchu. Restart komputera to naprawił. Jeśli backend znowu "nie widzi" zmian mimo poprawnego kodu: sprawdzić `Get-NetTCPConnection -LocalPort 8000` pod kątem więcej niż jednego `OwningProcess`, nie tylko `netstat`.
  - Zweryfikowane end-to-end w przeglądarce: nowe konto ma stronę główną od razu, przypinanie/odpinanie, kolor karty = kolor zakładki źródłowej, klik przenosi na właściwą zakładkę, przeciąganie realną myszą zmienia i persystuje kolejność pinów, usunięcie oryginalnego widgetu kasuje pin (kaskada FK) bez błędu, backend odrzuca (`400`) usunięcie strony głównej i dodanie na niej natywnego pola.
- **Okno ustawień** (`components/SettingsModal.tsx`, nowe) - kafelek konta w `Sidebar.tsx` (`.user-btn`, teraz `<button>` zamiast `<div>`) otwiera je zamiast być statyczny. Reużywa wzorca `.scrim`/`.modal` co reszta modali; na mobile (≤860px) nowa klasa `.settings-modal` + `.scrim--settings` robi z niego pełny ekran zamiast pływającej nakładki - za mało miejsca na to drugie przy wąskim viewporcie. `.settings-modal` ma też własne, szersze `max-width: 600px` na desktopie (zwykły modal ma 460px - za ciasno na dwuliniowe opisy wierszy); reguła mobile z `max-width: none` jest niżej w pliku, więc poprawnie wygrywa przy wąskim viewporcie. Treść: pogrupowana, przewijana lista w stylu "Ustawień" iOS/Windows - pogrubiony nagłówek sekcji, pod nim wiersze w jednej zaokrąglonej ramce z separatorami (`.settings-section`/`.settings-list`/`.settings-row` w `theme.css`). Sekcje (na razie same wiersze, bez podpiętego działania - klikalne, ale bez efektu, dochodzą po kolei w kolejnych krokach):
  - **Profil** - awatar (kolor kwadracika z inicjałem, ten sam mechanizm co wybór koloru zakładki - bez prawdziwego uploadu zdjęcia), imię i nazwisko (dziś `User` ma tylko `email` - potrzebna nowa kolumna).
  - **Konto** - zmiana hasła, zmiana e-maila, usunięcie konta (na czerwono, `.settings-row--danger`, kolor `#e0655f` jak `.btn-danger`).
  - **Aplikacja** - motyw (jasny/ciemny - duplikat przełącznika z sidebara, zostaje też tam dla szybkiego dostępu), o aplikacji.
  - **Dane** - eksport do JSON (pełna kopia, do importu z powrotem), eksport do CSV (czytelny, bez dodatkowej biblioteki jak przy prawdziwym `.xlsx`), import z JSON.
  - **Waluta** - domyślna waluta docelowa dla nowych pól walutowych.
  - Zweryfikowane w przeglądarce na desktopie (modal faktycznie szerszy, nagłówek przypięty, treść przewija się realnym scrollem myszy - `bodyScrollHeight`/`bodyClientHeight` potwierdzone przez JS) i na 375px (pełny ekran, naturalny scroll strony).
  - **Poprawka**: ostatnia sekcja ("Waluta") stykała się z krawędzią modala bez marginesu na dole - `.modal-body` (niżej w `theme.css`) ma w swoim skrócie `padding: 4px 24px 0`, czyli `padding-bottom: 0`, a moja klasa `.settings-body { padding-bottom: 24px }` była zadeklarowana WCZEŚNIEJ w pliku, więc przy równej specyficzności (obie to pojedyncza klasa) przegrywała z późniejszą regułą `.modal-body`, mimo że element ma obie klasy naraz. Naprawione podbiciem specyficzności selektora do `.modal-body.settings-body { padding-bottom: 24px }` zamiast przestawiania kolejności reguł w pliku - działa niezależnie od tego, która reguła jest zadeklarowana później.
  - **Znacznik "jeszcze nie zrobione"** (`SettingsRow`, prop `done?: boolean`, domyślnie `false`) - mała, przerywana etykietka obok nazwy wiersza, wyłącznie do orientacji w trakcie budowania (nie dla końcowego użytkownika - użytkownik poprosił o to explicite "dla siebie"). Przy wdrażaniu konkretnego ustawienia: dopisać `onClick` i przestawić `done={true}`, żeby etykietka zniknęła.
- **Profil: awatar (kolor + kształt) i imię/nazwisko** - pierwsze dwa realnie wdrożone ustawienia, jednocześnie pierwsza implementacja mechanizmu nawigacji w oknie ustawień.
  - **Nawigacja - trzy podejścia odrzucone, zanim trafiliśmy we właściwe.** (1) "Wstecz + widok szczegółowy" (podmiana całej treści modala) - technicznie NIE stackowało `.scrim`/`.modal` (dalej jeden modal, potwierdzone przez `document.querySelectorAll('.scrim'/'.modal').length`), ale użytkownik i tak odebrał to jako "otwieranie nowego modala", bo cała lista znikała. (2) Akordeon (`expandedRow` state, wiersz rozwija swoją treść W MIEJSCU pod sobą, reszta listy zostaje widoczna) - też odrzucone, mimo że działało technicznie poprawnie (jeden rozwinięty naraz, chevron obraca się o 90°). (3) Panel boczny z kategoriami (master-detail jak Slack/Discord/VSCode, wzorowany na tym jak Ledger już ma listę zakładek + treść) - odrzucone na starcie, zanim zbudowane, bo przy 5 kategoriach i 1-3 ustawieniach w każdej wyglądałoby na zbyt "ciężkie" narzędzie do zbyt małej ilości treści.
  - **Właściwy kierunek: dopasować mechanizm do konkretnego ustawienia, nie jeden uniwersalny wzorzec na wszystko** - proste pojedyncze wybory (imię, motyw, domyślna waluta) dostają kontrolkę wprost w wierszu, bez żadnego rozwijania; eksport/import to od razu AKCJA (klik = ściąga/otwiera wybór pliku), nie "ustawienie do skonfigurowania"; awatar (7 kolorów × 10 kształtów, za dużo na wiersz) dostanie małe okienko-popover przy kliknięciu, nie rozpychanie listy; zmiana hasła/e-maila/usunięcie konta (wielopolowe albo wymagające potwierdzenia, zwłaszcza usuwanie) dostają osobny, mały modal - dokładnie tak jak `TabModal`/`EntryModal` już działają gdzie indziej w apce, więc to nic nowego architektonicznie. Do zbudowania per ustawienie, po kolei.
  - **Poprawka wizualna**: `.settings-list` miała własną ramkę (`border` + `border-radius` + `overflow:hidden`) otaczającą całą sekcję - na prośbę użytkownika zdjęta, zostają tylko cienkie linie (`border-bottom` na `.settings-row-wrap`) między wierszami, bez zamykającego boxa wokół sekcji. Przy okazji `.settings-section-title` powiększone z 13px na 16px (przy 14px wierszy różnica samego pogrubienia nie wystarczała, żeby nagłówki sekcji wyraźnie się odróżniały).
  - **Kontrolki wprost w wierszu, wdrożone dla wszystkich 11 wierszy jako podgląd całości** - `SettingsRow` dostał prop `control?: ReactNode`; z nim wiersz to zwykły `<div>` (nie jeden duży `<button>` jak wcześniej - kontrolka to własny, niezależny element interaktywny, więc np. pole tekstowe da się normalnie zaznaczać/edytować). Bez `control` wiersz zostaje przyciskiem z chevronem (na razie tylko Konto: zmiana hasła/e-maila/usunięcie konta - docelowo osobny mały modal, patrz niżej).
    - **Awatar** i **Imię i nazwisko** - REALNIE działające (mają już backend), tylko zmieniona prezentacja: `AvatarRowControl` to mały klikalny podgląd `Avatar` (30px), otwiera `.settings-popover` (pływający panel z `AvatarPicker` w środku, zamyka się po zapisie albo kliknięciu poza sobą - `useEffect` + `mousedown` na `document`, sprawdzane przez `ref.contains`); `NameRowControl` to zwykły `<input>` w wierszu, zapisuje się sam na `onBlur` (bez osobnego przycisku Zapisz - za mało tu, żeby uzasadnić cały formularz).
    - **Reszta (Motyw, O aplikacji, Eksport/CSV/Import, Domyślna waluta)** - CELOWO tylko wizualne makiety do podglądu całości, jeszcze niepodpięte pod realne działanie: `ThemeRowMock` (prawdziwy stan z `getInitialTheme()`, ale klik nie wywołuje `applyTheme`), `CurrencyRowMock` (prawdziwa lista z `lib/currencies.ts`, wybór się nie zapisuje), przyciski `Download`/`Upload` z `.field-icon-btn` (reużyte, bez nowego CSS) dla eksportu/importu - te są AKCJAMI (klik = od razu coś się dzieje), nie ustawieniami do skonfigurowania, więc dostają ikonę zamiast chevronu czy pola. Wszystkie zostają z `done` domyślnie `false` (etykietka "jeszcze nie zrobione").
    - Zweryfikowane w przeglądarce: popover awatara otwiera/zamyka się poprawnie (też na 375px, mieści się w viewporcie dzięki `max-width: calc(100vw - 32px)`), zapis koloru+kształtu trafia do bazy, `NameRowControl` zapisuje się na blur i `Sidebar` aktualizuje się natychmiast.
  - **Trzy poprawki po pierwszym spojrzeniu użytkownika**:
    1. "Imię i nazwisko" → **"Imię"** - te dane i tak nie są nigdzie użytkownikowi potrzebne w pełnej formie ("pełne dane nam nie są potrzebne"), label/placeholder w `SettingsModal.tsx` zmienione; kolumna w bazie zostaje `name` (już wystarczająco generyczna, żadnej migracji nie trzeba).
    2. **Kafelki kształtu awatara za mało widoczne, zwłaszcza na mobile** - `.avatar-shape` miało `background: var(--hover-bg)` (tylko 7% biały nakład w dark mode - prawie znikało). Zmienione na `var(--input-bg)` (ten sam token co zwykłe pola formularza, dużo bardziej kontrastowy tak w jasnym, jak i ciemnym motywie).
    3. **Podświetlenie `:hover` "zamraża się" na dotyku (mobile)** - klasyczny problem: na urządzeniach dotykowych nie ma zdarzenia `mouseout`, więc raz nałożony `:hover` zostaje aż do dotknięcia gdzieś indziej. Zgłoszone przy `.settings-row`, ale to samo dotyczyło już wcześniej istniejących `.theme-toggle`, `.tab-gear`, `.edit-mode-btn`, `.user-btn`, `.field-icon-btn` - żaden nie miał zabezpieczenia. Naprawione ogólnie (user: "możemy to ogólnie chyba wywalić") - wszystkie `:hover` w `theme.css` owinięte w `@media (hover: hover)`, więc aplikują się tylko na urządzeniach z prawdziwym hoverem (myszka), nie na dotyku.
  - **Backend** - `User` (`models.py`) dostał `name`, `avatar_color`, `avatar_icon` (wszystkie `nullable=True`, migracja `b8be7b6fca8c_add_user_profile_fields.py`, bez backfillu - `NULL` to poprawny stan "nic nie ustawione"). Nowy `PATCH /auth/me` (`routers/auth.py`) obok istniejącego `GET` - `exclude_unset`, ten sam wzorzec co `update_tab`/`update_widget`. `avatar_color`/`avatar_icon` bez walidacji enum po stronie backendu - tak samo luźno jak `Tab.color` dziś, frontend odpowiada za sensowne wartości.
  - **`lib/palette.ts`** - wyciągnięta z `TabModal.tsx` stała `PALETTE` (7 kolorów), teraz współdzielona między kolorami zakładek i awatara, żeby nie rozjeżdżały się w dwóch miejscach.
  - **`lib/avatarIcons.ts`** - `AVATAR_ICONS`, 9 gotowych ikon z `lucide-react` (już w projekcie, zero nowej zależności): `Star, Heart, Sun, Moon, Zap, Coffee, Cat, Rocket, Smile`. `avatar_icon: null` = litera-inicjał, 10. "kształt", nie wymaga wpisu w mapie.
  - **`components/Avatar.tsx`** (nowy, reużywalny) - renderuje kwadracik: tło `user.avatar_color ?? var(--brand-gradient)` (fallback = wygląd sprzed tej funkcji, nic się nie zmienia dla konta bez ustawień), środek = ikona z `AVATAR_ICONS[user.avatar_icon]` albo pierwsza litera `user.name || user.email`. Zastąpił inline `<span className="avatar">` w `Sidebar.tsx`.
  - **`components/AvatarPicker.tsx`** (nowy) - siatka kolorów (`.swatch-row`/`.swatch`, identyczne klasy co `TabModal`) + siatka 10 kształtów (`.avatar-shape-row`/`.avatar-shape`, nowe klasy w `theme.css`, wzorowane na `.swatch` ale z zaznaczeniem przez `--seg-thumb-bg` jak `.tab-item.is-active`), duży podgląd na żywo (`Avatar` w 64px) nad siatkami. Draft w stanie lokalnym, commit dopiero na "Zapisz" (ten sam wzorzec co `TabModal`) - powrót do listy w `SettingsModal` dzieje się automatycznie po udanym zapisie.
  - Imię i nazwisko - jedno pole (jedna kolumna `name`, nie firstName/lastName osobno - użytkownik mówił o tym jako o jednym ustawieniu). `Sidebar.tsx` pokazuje `user.name || user.email`.
  - Zweryfikowane end-to-end w przeglądarce: wybór koloru/kształtu widoczny na żywo w podglądzie, zapis trafia do bazy (`GET /auth/me` potwierdza), `Sidebar` aktualizuje się natychmiast bez przeładowania, powrót do listy po zapisie, strzałka wstecz i X działają niezależnie, `exclude_unset` nie nadpisuje awatara przy zapisie samego imienia (i odwrotnie), to samo na 375px (pełny ekran, nawigacja wstecz identyczna).
